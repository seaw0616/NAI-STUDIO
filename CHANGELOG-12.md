# 12.0 개발 기록

> v11.23 → v12.0 사이의 준비 빌드(11.99.1~18) 기록입니다.
> 사용자용 릴리스 노트는 깃헙 릴리스 페이지에 있습니다. 이 문서는 "왜 그렇게 고쳤는가" 를 남깁니다.

---

## 11.99.11 — 떠 있는 것들이 화면을 덮던 문제 · 스마트 툴 레이아웃

### ⚠ 스마트 툴 탭이 화면의 1/4 만 쓰고 있었습니다

`씬 모드`·`라이브러리` 에는 있는 레이아웃 규칙이 **스마트 툴에만 없었습니다.** 그래서 메인 화면의 3단 격자가 그대로 남아, 툴 화면이 첫 칸(370px)에 들어가 있었습니다.

1600×900 에서 잰 값입니다.

| | 전 | 후 |
|---|---|---|
| 스마트 툴 화면 | 370px | **1576px** |
| 이미지 넣는 칸 | **15px** | **1221px** |

`overflow: hidden` 이 걸려 있어 잘린 부분에 스크롤로 닿을 수도 없었습니다. 라이브러리도 좁은 화면에서 아래 절반이 비던 것을 같이 고쳤습니다.

### 태그 자동완성이 아래 칸을 덮던 문제

무조건 칸 **아래**에 붙이고 있었습니다. 이제 아래에 자리가 없으면 **위로** 올라갑니다. 양쪽 다 좁으면 넓은 쪽에 붙이고 높이를 줄여 화면 밖으로 안 나가게 합니다.

6개 칸에서 재보니 위쪽 2개는 아래로, 아래쪽 4개는 위로 뜨고 전부 화면 안입니다.

**스크롤을 따라오지 않던 것도 고쳤습니다.** 목록을 띄운 채 휠을 굴리면 박스만 제자리에 남아 엉뚱한 곳을 덮었습니다. 청크 칩 띠는 같은 이유로 이미 처리돼 있었는데 자동완성만 빠져 있었습니다.

### 우클릭 메뉴가 화면 밖으로 나가던 문제

가로를 **상수 200px** 로 막고 있었는데 실제 폭은 **237px** 입니다 (`🗑 분류 삭제 (청크는 "기본"으로 이동)` 같은 긴 항목 때문). 오른쪽 끝에서 열면 37px 이 잘렸고, `body` 에 `overflow: hidden` 이라 스크롤로도 못 봤습니다. 하필 잘리는 게 삭제 항목의 끝이었습니다.

- 실제 크기를 재서 자리를 잡습니다 (네 구석 전부 화면 안 확인)
- 아래가 좁으면 커서 **위**로 올립니다
- **Esc** 로도 닫힙니다 (전에는 바깥 클릭뿐)

> 같은 유형이 네 번째라(청크 칩 띠 → 유튜브 창 → 자동완성 → 우클릭 메뉴) 떠 있는 요소를 전부 훑었습니다. 모달·토스트·툴팁은 문제가 없었습니다 — 모달은 크기를 CSS 로만 잡아 창을 줄여도 알아서 맞고, 토스트는 `pointer-events: none` 이라 덮어도 클릭을 막지 않습니다.

---

## 11.99.10 — 태그 자동완성 위치 (11.99.11 에 포함)

---

## 11.99.9 — 저장·버튼이 느리던 문제

사장님 상태를 그대로 복제해서 쟀습니다 (청크 77개 · 대기열 280곡 · 149KB).

| | 전 | 후 |
|---|---|---|
| **syncUI** (버튼 누를 때마다) | **63ms** | **8.5ms** |
| refreshHighlights | 16.5ms | **0.2ms** |
| renderChunkBar | 18.1ms | 8.3ms |
| updatePreview | 14.6ms | 6.4ms |
| save | 8.6ms | 4.9ms |

**원인은 청크였습니다.** 프롬프트의 토큰 하나를 볼 때마다 청크 전체를 훑고, 그때마다 이름 정규화를 다시 계산했습니다. 청크가 늘수록 그대로 느려지는 구조였습니다 (0개 0.057ms · 77개 0.464ms · 200개 1.089ms — 정확히 비례).

이름→청크를 한 번만 만들어 재사용하고, **내용이 안 바뀐 칸은 다시 그리지 않습니다.**

### 한글 태그 검색도 50배 빨라졌습니다

결과가 8건 미만이면 22만 행을 전부 훑는데, 한글은 대개 결과가 적어 거의 항상 그 경로였습니다. 글자를 더할수록 결과는 줄어들 뿐이니 **직전 후보 안에서만** 찾게 했습니다.

```
흰머 → 흰머리   한 글자 더:  0.01ms
```

> 21개 질의를 "한 번에 친 것 vs 이어서 친 것" 으로 대조해 결과가 같은지 확인했습니다.

---

## 11.99.8 — 유튜브 창 크기 조절 · 좁은 화면 정리

### 유튜브 창을 모서리로 끌어 크기 조절

오른쪽 아래 모서리에 손잡이를 달았습니다. **더블클릭하면 원래대로** 돌아갑니다.

- 창이 오른쪽 끝에 붙어 있어도 커집니다 — 오른쪽으로 끌면 **왼쪽 모서리가 밀리면서** 실제로 커집니다
  > 처음 만들 때는 "지금 자리에서 화면 끝까지"로 제한했더니 기본 위치에서 12px 밖에 안 늘어났습니다. 손잡이가 안 듣는 것처럼 보였을 겁니다
- 늘린 높이를 **목록이 채웁니다** (700px 로 키우면 목록이 454px 로 늘고 그 안에서 스크롤)
- 최소 240×120 에서 멈추고, 화면 밖으로 나가지 않습니다
- 큰 모니터에서 크게 해두고 작은 화면에서 열면 알아서 줄여 넣습니다

### 좁은 화면에서 유튜브 창이 폭 전체를 먹던 것

900px 미만 규칙이 `!important` 로 **compact 까지 덮어써서**, 컨트롤만 있는 얇은 띠가 화면을 가로질렀습니다. 이제 compact 와 직접 정한 크기는 지킵니다.

### 3단이 너무 일찍 무너지던 것

브라우저를 1300px 쯤 띄워도 **디스플레이 배율 때문에 CSS 기준으로는 900 아래**로 떨어지는 일이 흔합니다. 그러면 프롬프트 칸 옆의 다른 칸이 전부 아래로 내려가 페이지가 세로로 길어집니다.

860px 에서 2단이 가로 넘침 없이 버티는 것을 재고 경계를 낮췄습니다.

| 폭 | 전 | 후 |
|---|---|---|
| 1400 이상 | 3단 | 3단 |
| 1100~1400 | 3단 | 3단 |
| 900~1100 | 2단 | 2단 |
| **760~900** | **세로로 쌓임** | **2단** |
| 760 미만 | 세로로 쌓임 | 세로로 쌓임 |

### ⚙설정 버튼이 화면 밖으로 잘리던 것

상단 알약 버튼 줄이 `flex-shrink: 0` 이라 줄어들지도 접히지도 못해, 마지막 버튼(하필 ⚙설정)이 잘렸습니다. 이제 좁으면 **한 줄 더 내려갑니다.**

> 가로 스크롤로도 풀리지만 그러면 버튼이 여전히 안 보입니다. 스크롤해야 나오는 걸 알아채기 어렵고요.

**실측 (1600 · 1400 · 1200 · 1000 · 860 · 780 · 740 · 560):** 가로 넘침 0건, ⚙설정 항상 보임.

---

## 11.99.7 — 지워진 영상 · 청크 칩 띠 기본값

### 청크 칩 띠, 이제 기본으로 안 뜹니다

프롬프트 칸을 클릭만 해도 청크 칩이 떠서 아래 칸을 덮는다는 얘기가 **두 번** 나왔습니다.
✕ 와 설정 스위치를 달아뒀지만(동작은 정상입니다), 끄는 법을 안내할 게 아니라 **기본값이 잘못된** 것이었습니다.

- 기본을 **안 뜸**으로 바꿨습니다. 필요하면 ⚙설정 → **청크 칩 띠 띄우기** 로 켭니다
- 📌 로 고정해 쓰시던 분은 켠 채로 넘어갑니다 (즐겨 쓰신 것이니까요)

> 설정 키 이름을 바꿨습니다. 안 그러면 이미 저장돼 있던 값이 이겨서 기존 사용자에게는 아무 변화가 없습니다.

### 지워진 영상에서 세 가지가 어긋나던 것

오류 기록 한 줄에 문제가 셋이었습니다.

```
스트림 준비 실패 S25S9kK41aU no playable stream ([0;31mERROR:[0m ... Video unavailable)
 · NAI 공식 상태: 이미지 생성 Degraded Performance (NAI 쪽 문제입니다)
```

- **`[0;31m`** — yt-dlp 가 터미널용으로 넣는 색상 코드가 그대로 새어 나왔습니다. 지웁니다
- **NAI 상태가 붙었습니다** — 유튜브 영상이 지워진 것뿐인데 NAI 이미지 생성 장애를 함께 알려서, 엉뚱한 데를 의심하게 만들었습니다. 로컬 서버는 유튜브·Gemini 실패도 502 로 돌려주는데, 500번대면 무조건 NAI 상태를 붙이고 있었습니다. 이제 **NAI 주소일 때만** 붙입니다
- **지워진 영상을 계속 다시 시도했습니다** — 몇 번을 해도 안 되는데 클라이언트 6개를 다 돌았습니다. 이제 알아보고 즉시 멈춥니다

| | 전 | 후 |
|---|---|---|
| 지워진 영상 판정 | ~10초 (6개 클라이언트 순회) | **2.1초** |
| 그 뒤 | 오류 패널 띄우고 멈춤 | **0.8초 뒤 다음 곡** |

지워진 영상은 **연속 10곡까지** 자동으로 넘어갑니다 — 재생목록에 죽은 링크가 여럿 섞이는 건 흔한 일이고, 판정이 2초면 끝나니까요. (재생이 안 되는 다른 이유는 그대로 3곡에서 멈춥니다)

---

## 11.99.6 — 가중치 강조 표시 · 대기열이 안 멈춥니다

### 강조/비강조를 프롬프트 칸에서 보여줍니다 (NAI 화면처럼)

`1.2::red hair, blue eyes::` 같은 묶음이 화면에서 전혀 안 보였습니다. 잘못 걸어놔도 눈치채기 어려웠고요.

- **올린 건 빨강, 내린 건 파랑**. 진할수록 세게 준 것입니다
- `1.2::tag::` · `-0.8::tag::` · `{tag}` · `{{tag}}` · `[tag]` · `[[tag]]` 전부 인식합니다
- `1.2::red hair, blue eyes::` 처럼 **쉼표를 품은 묶음도 한 덩어리**로 잡습니다
- 안쪽 청크 칩 색은 **그대로 같이 보입니다**
- `1::tag::`(가중치 1)나 `{{tag}`(짝이 안 맞음)는 표시하지 않습니다
- ⚙설정 → **가중치 강조 표시** 로 끌 수 있습니다. NAI 도 기본 켬이라 기본값은 켬입니다

> 끄면 화면 출력이 예전과 **글자 하나까지 동일**합니다 (31가지 입력으로 확인했습니다).

### 대기열이 한 곡에서 멈추던 문제

곡이 끝나면 다음으로 넘어가는 부분은 멀쩡했습니다. **곡이 실패했을 때 넘어가는 코드가 없어서** 거기 주차됐습니다.

멈추는 자리가 두 군데였는데, 그중 하나는 **기록조차 안 남겨서** 아무 흔적 없이 멈췄습니다.

실제 기록으로 확인한 것:

| | |
|---|---|
| 실패가 없던 구간 | 16곡·7곡이 곡 길이 간격으로 착착 진행 |
| 실패 직후 | **8,882초 / 2,023초 / 27,120초** 멈춤 |

이제 **5초 뒤 자동으로 다음 곡**으로 넘어갑니다. 다만 전부 실패하는 상황에서 대기열 300곡을 몇 초 만에 태워버리면 안 되므로, **연속 3곡까지만** 자동으로 넘기고 그 뒤엔 손을 뗍니다. 한 곡이라도 실제로 재생되면 카운터가 초기화됩니다.

> 사장님이 직접 다른 곡을 고르시면 예약된 자동 넘김은 취소됩니다.

---

## 11.99.5 — 채점 기준 손봄

### Gemini 채점이 너무 후하던 것

지시에 기준이 `10 = flawless, 0 = badly broken` 한 줄뿐이었습니다. 기준점 없이 점수만 물으면 비전 모델은 거의 항상 8~9를 줍니다. 게다가 **결함을 찾게 시키지 않고 바로 점수를 내라**고 해서, 꼼꼼히 안 보고 인상으로 답했습니다.

- **점수보다 결함을 먼저 나열**하게 했습니다 — 찾는 행위를 강제해야 실제로 봅니다
- **손가락을 세어 숫자로 적게** 했습니다 (`왼손 6개` 처럼)
- 점수 구간을 못박았습니다 — `9 = 눈치 못 챌 트집` / `5 = 한눈에 보임` / `3 = 다시 뽑아야 함`
- **종합은 가장 낮은 항목을 못 넘습니다.** 손이 3점인데 종합 9점이 나오던 걸 막습니다
- 찾은 결함이 **목록으로** 나옵니다 — 점수만 보면 왜 그 점수인지 알 수 없습니다

### 자체 그림 품질 판정이 너무 빡빡하던 것

세 지표(선명도·대비·다양성) 중 **하나만** 낮아도 노란불이었습니다. 다양성은 평소보다 10%만 낮아도 걸렸고요 — 어두운 장면이나 단순한 배경이면 그냥 넘어갑니다.

한 지표만 낮은 건 그림 차이입니다. 모델이 진짜 망가지면 여러 지표가 **함께** 내려갑니다. 이제 두 개 이상일 때만 알립니다.

| 상황 | 전 | 후 |
|---|---|---|
| 다양성만 12% 낮음 | 노랑 | **초록** |
| 대비만 25% 낮음 | 노랑 | **초록** |
| 둘이 함께 낮음 | 노랑 | 노랑 |
| 뭉개짐(진짜 이상) | 빨강 | 빨강 |

---

## 11.99.4 — 생성이 막히던 것 (제가 낸 오류)

11.99.2 에서 V5 인페인트를 고치다가, **다른 함수에 있는 변수**를 가져다 썼습니다. 그 줄이 대부분의 생성에서 실행되는 자리라 **생성이 통째로 막혔습니다.** 고쳤고, 이번엔 9개 경로(t2i·i2i·인페인트 × V5 Full·V5 Curated·V4.5)를 전부 실제로 돌려 확인했습니다.

---

## 11.99.3 — 앱 파일을 갈아끼웠는데 안 껐다 켠 것을 알아챕니다

두 번 겪었습니다. 유튜브 재생 문제를 고쳐 파일까지 넣어뒀는데 오류 보고가 계속 왔고,
보니 **돌고 있는 서버가 옛 버전**이었습니다. 보고서에도 그렇게 찍혀 있었고요 — `11.23 (앱 11.99.1)`.

브라우저는 디스크에서 새 화면 파일을 바로 읽어 갑니다. 그런데 서버는 **켤 때 읽은 코드를 계속 씁니다.**
그래서 화면만 새것이고 서버가 하는 일(유튜브 재생 같은)은 옛것인 상태가 됩니다. 알아채기가 어렵습니다.

이제 서버가 스스로 압니다. 켠 뒤에 앱 파일이 바뀌면 상단 ⚠ 에
**"앱을 껐다 켜야 적용됩니다 (지금은 옛 코드가 돌고 있습니다)"** 가 **즉시** 뜹니다.

---

## 11.99.2 — V5 정식 공개본 대조

V5 지원은 V5 가 **테스트 단계**일 때 조사한 정보로 만들어져 있었습니다.
오늘 정식 공개된 novelai.net 을 직접 받아 대조하니 **8군데가 어긋나** 있었습니다.
(라이브 번들 원문 대조 + 실제 V5 샘플 메타데이터 81건 교차확인)

### 그림이 달라지던 것

- **noise_schedule 이 빠져 있었습니다.** NAI 의 능력치 표만 보면 V5 는 이 값을 안 받는 것처럼 되어 있는데, 웹은 그렇게 지운 뒤 **V5 에만 `karras` 를 도로 넣습니다.** 실제 V5 그림 81장의 메타데이터가 전부 karras 였습니다. 앱은 이 한 줄을 놓쳐서 NAI 웹과 다른 요청을 보내고 있었습니다
- **투명 배경이 사실상 동작하지 않았습니다.** 앱은 플래그 두 개만 보냈는데, 그 중 `tag_hint_transparent_background` 는 서버가 해석하지 않는 단순 전달용입니다. 웹은 **프롬프트에 `transparent background` 를 직접 넣습니다.** 이제 앱도 같은 자리(퀄리티 태그 앞)에 넣습니다
- **V5 Curated 에 `nsfw` 가 몰래 붙고 있었습니다.** 웹은 Curated 계열에 자동 nsfw 를 붙이지 않는데, 앱의 면제 목록에 V5 Curated 가 빠져 있었습니다
- **V5 기본 프롬프트 가이던스가 5 였습니다.** V5 는 **7** 입니다. 이제 모델을 바꾸면 그 모델의 기본값을 따라갑니다 — 단, 직접 만져둔 값은 그대로 둡니다
- 기본 스텝을 **28 → 23** 으로. NAI 는 V3 부터 쭉 23 입니다 (기존 사용자 설정은 그대로)

### 요청이 거절되던 것

- **V5 Curated 인페인트 모델은 아직 안 나왔습니다.** 웹은 이 조합을 4.5 Curated 인페인트로 보냅니다. 앱은 없는 모델 이름을 보내고 있었습니다
- **V5 인페인트에서 바이브·캐릭터 레퍼런스가 안 걸러졌습니다.** 인페인트용 모델 이름으로 능력치를 찾다 보니 아무것도 못 찾고 V4.5 값으로 떨어졌고, V5 가 안 받는 것들이 그대로 실려 나갔습니다

### 그 밖에

- **Curated 로 뽑은 그림을 불러오면 전부 Full 로 잡혔습니다.** PNG 에 적히는 이름은 `NovelAI Diffusion V5 DB276663` 처럼 **해시**라서 "curated" 라는 단어가 아예 없습니다. 웹과 같게 해시로 가릅니다 (V4·V4.5 도 같은 문제였습니다)
- **V5 Anlas 가 실제의 2/3 로 표시됐습니다.** V5 는 1.5 배입니다

> 앱이 맞게 하고 있던 것도 16가지 확인했습니다 — 모델 ID, `params_version` 4, 토큰 한도(Full 1471 · Curated 703), 캐릭터 32명, 해상도 프리셋, UC·퀄리티 프리셋 원문, Opus 무료 한 장 규칙 등.

---

## 11.99.1 — 태그 한글 검색

### 한글 별칭 7,581개 추가

지금까지 영어로만 검색되던 태그들에 한글 뜻을 붙였습니다.
`옵치` 로 오버워치가, `베레스` 로 파이어 엠블렘 캐릭터가, `엣지러너` 로 사이버펑크가 나옵니다.

- 기본 사전에 6,035개, 작가·캐릭터 사전에 1,546개
- **사용량 기준 94%** 를 덮습니다. 나머지는 대부분 100회 미만으로 쓰이는 희귀 태그라 영어 이름 그대로 검색됩니다

넣지 않은 것도 있습니다. `\||/` 같은 기호 태그는 뜻을 확신할 수 없어서 비워 뒀습니다.
틀린 한글 뜻을 붙이면 검색이 오히려 더 나빠집니다.

### 어미가 달라도 찾습니다

`앉아있는` 으로 검색하면 아무것도 안 나왔습니다. 사전에는 `앉아 있음` 이라고만 적혀 있었거든요.
사람은 `앉아있는`, `앉은`, `앉기` 를 섞어 쓰는데 사전은 하나로만 적을 수밖에 없습니다.

- 자주 쓰는 동작 태그 38개에 어미형을 직접 넣었습니다 (`서있는`, `뛰는`, `눈감은`, `팔짱 낀` …)
- 그 밖의 말은 **검색이 알아서 받아줍니다** — 결과가 모자라면 끝 글자를 하나 떼고 다시 찾습니다

측정한 결과 39개 질의 중 **39개가 상위 8위 안, 37개가 1위** 로 나옵니다. (전에는 `서있는`·`앉아있는`·`뛰는` 이 아예 0건이었습니다)

### 검색 순위도 손봤습니다

`앉아있는` 을 치면 `sitting_on_mushroom`(수천 건)이 `sitting`(119만 건)보다 위에 있었습니다.
어미를 떼고 찾은 결과에 점수를 평평하게 주는 바람에 희귀 태그가 정답을 눌렀던 겁니다. 이제 제자리를 찾습니다.

### 사전 오역 정리

- **알파벳을 한글로 읽어 놓은 것** 을 걷어냈습니다 — `엠식스티`, `씨포`, `에이케이105` 로 검색하는 사람은 없습니다. `M60`, `C4`, `AK-105` 로 되돌렸습니다
  (`슈투카`, `데크탸료프`, `포케불프` 처럼 원래 한글로 적는 이름은 그대로 뒀습니다)
- 뜻이 다른 별칭 제거 — `pink_garter → 대님`(한복 대님), `socket_wrench → 스패너`(다른 공구), `falling → 손으로 부채질하며 말하다`

### ⚠ 업데이트해도 사전이 안 바뀌던 문제

앱은 태그 사전을 **첫 실행 때 한 번만** 데이터 폴더로 복사하고, 그 뒤로는 쳐다보지 않았습니다.
그래서 이미 쓰고 계신 분은 앱을 업데이트해도 **사전이 첫 설치 때 것 그대로** 였습니다.

위의 번역을 아무리 넣어도 새로 설치한 사람에게만 전달됐을 겁니다. 이제 사전이 바뀌면 켤 때 갈아끼웁니다.

> 직접 받아 만든 더 큰 사전을 쓰고 계시면 그건 건드리지 않습니다.

---

## (같은 빌드) 유튜브 재생이 몇 초 만에 끊기던 문제

**사장님이 보내주신 오류 보고로 잡았습니다.** 진단 정보에 `avc1+mp4a 360p` 라고 찍혀 있어서
"코덱 문제가 아니다" 를 바로 알 수 있었고, 거기서 출발했습니다.

원인은 유튜브가 요즘 상당수 스트림에 요구하는 **PO Token** 입니다. 없으면 앞의 몇 MB 만 주고 그 뒤로는 거부합니다.

앱은 재생 주소를 고를 때 **앞 1KB 만 확인** 했습니다. 막히는 주소도 앞부분은 멀쩡히 주니까 검사를 통과했고,
정작 끝까지 되는 주소는 시도조차 안 했습니다. 그래서 재생은 시작되는데 몇 초 뒤 끊겼습니다.

이제 **앞과 뒤를 둘 다** 확인합니다. 뒤가 막히면 그 주소를 버리고 다음 것을 씁니다.

| | 전 | 후 |
|---|---|---|
| 문제의 영상 (14.8MB) | 4MB 에서 중단 | **전부 받음** |

> 조각 크기를 줄이거나 요청 방식을 바꿔 봐도 소용없었습니다 (네 가지를 다 재봤고 전부 같은 지점에서 막혔습니다).
> 재생 주소를 고르는 단계에서 걸러내야 하는 문제였습니다.

---

## (같은 빌드) 태그 입력이 손에 안 붙던 것들

### 쓰는 도중 태그가 사라지던 문제

`white shirts` 를 찾으려고 `white s` 까지 쳤을 때 셔츠가 빼꼼 보이다 사라지고
다른 태그가 치고 올라왔습니다. 목록이 갱신될 때마다 통째로 다시 그려서 그랬습니다.
이제 **이미 보이던 것은 자리를 지킵니다.**

### 프롬프트만 쓰는데 청크 칩 띠가 나오던 문제

- 띠에 **✕** 를 달았습니다. 거슬리면 그 자리에서 끄면 됩니다
- ⚙설정에도 스위치를 뒀습니다 (`청크 칩 띠 안 띄우기`)
- 띠가 **입력칸을 가리던** 것도 고쳤습니다 — 아래에 자리가 없으면 위로 띄웁니다

---

## 그 밖에 (11.23 에 이미 반영)

- 소스 업데이트가 `.bat` 파일 줄바꿈을 바꾸던 것 정리
  > 앱이 안 켜질까 걱정하실 만한 일이었는데, **실제로는 문제가 없었습니다.** 확인해 보니 두 방식 다 정상 동작합니다. 정리 차원의 변경입니다

---

## 12.0 배포 전에 할 일

- [ ] `verify_exe.py` 로 빌드 검사 (번들 파일 + 실제 실행)
- [ ] 개인정보 스캔 (`deep_scan.py`)
- [ ] 검색 벤치마크 재확인 — 현재 **20/20** (기준선 17/20)
- [ ] 릴리스 노트 작성
